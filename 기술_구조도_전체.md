# IFRO 서울 교통 플랫폼 - 기술 구조도 및 기능 흐름

## 1. 시스템 구조 개요

### 1.1 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                    클라이언트 계층 (Frontend)                       │
│                   (React + TypeScript + Tailwind)                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                              ↕ HTTP/REST
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                 API 게이트웨이 & 백엔드 서비스                      │
│                   (Django + Django Ninja)                           │
│                                                                     │
│  - 사용자 인증 (JWT)                                               │
│  - 교통 데이터 API                                                 │
│  - 챗봇 프록시 (FastAPI 연계)                                     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                              ↕ HTTP/REST
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│             AI/ML 서비스 계층 (Chatbot v6 + Ollama)                │
│              (RAG 기반 챗봇 with FastAPI + Python)                │
│                                                                     │
│  - 문서 처리 (PDF 추출 + OCR)                                     │
│  - 임베딩 & 벡터 DB (Chroma)                                     │
│  - RAG 파이프라인                                                 │
│  - LLM (Ollama - Qwen2.5)                                         │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                              ↕ SQL/Socket
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                      데이터 저장소 계층                            │
│                                                                     │
│  - 관계형 DB (MySQL/SQLite)                                       │
│    : 사용자, 교통 데이터, 사건, 교차로 정보                       │
│  - 벡터 DB (Chroma - SQLite 기반)                                │
│    : 임베디드 문서 청크 저장                                      │
│  - 파일 저장소                                                     │
│    : PDF 문서, 캐시, 로그                                         │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 2. 마이크로서비스 구조

### 2.1 프론트엔드 서비스 (React)
**경로**: `django-react-frontend-ifro/`

```
Frontend (React + TypeScript)
├── UI Components
│   ├── 인증 (로그인/회원가입)
│   ├── 대시보드 (지도 기반)
│   ├── 교차로 상세보기
│   ├── 사건 리포팅
│   └── 채팅 인터페이스 (챗봇)
├── API 통신
│   ├── axios 인스턴스
│   ├── JWT 토큰 자동 첨부
│   └── 자동 리프레시 토큰 관리
├── 상태 관리
│   ├── Redux (선택사항)
│   └── Context API
└── 다국어 지원
    └── i18n (영어, 한국어 등)
```

**주요 기능**:
- 반응형 디자인 (Tailwind CSS)
- shadcn UI 컴포넌트
- JWT 기반 인증
- 지도 API 통합
- 실시간 데이터 업데이트

---

### 2.2 백엔드 API 서비스 (Django)
**경로**: `django-react-backend-api-ifro/`

```
Backend (Django + Django Ninja)
├── 사용자 인증 (JWT)
│   ├── 회원가입 (username/password)
│   ├── 로그인 (JWT 토큰 발급)
│   ├── Access Token (60분)
│   └── Refresh Token (7일)
├── 교통 데이터 API
│   ├── Intersections (교차로 정보)
│   │   ├── GET /api/traffic/intersections/
│   │   ├── POST /api/traffic/intersections/
│   │   └── 구역별 조회
│   ├── TrafficVolume (교통량)
│   │   ├── 시간대별 데이터
│   │   └── 추세 분석
│   ├── Incidents (사건/사고)
│   │   ├── 실시간 리포팅
│   │   └── 기록 관리
│   └── FlowMetrics (흐름 지표)
│       ├── 혼잡도
│       └── 평균 속도
├── 챗봇 프록시
│   ├── FastAPI 챗봇 요청 중계
│   ├── 응답 변환 & 포맷팅
│   └── 오류 처리
├── 데이터 모델
│   ├── User (사용자)
│   ├── Intersection (교차로)
│   ├── TrafficVolume (교통량)
│   ├── Incident (사건)
│   └── FlowMetrics (흐름 지표)
└── 관리 커맨드
    ├── 마이그레이션
    ├── 데이터 로드
    └── 초기화
```

**API 엔드포인트 구조**:
```
/api/
├── auth/
│   ├── register/
│   ├── login/
│   ├── refresh/
│   └── me/
├── traffic/
│   ├── intersections/
│   ├── traffic-volumes/
│   ├── incidents/
│   └── flow-metrics/
└── chatbot/
    ├── ask/
    ├── pdf/
    └── status/
```

---

### 2.3 AI/ML 챗봇 서비스 (FastAPI)
**경로**: `Chatbot_v6/`

```
Chatbot v6 (FastAPI + Python)
├── 입력 처리 계층
│   ├── PDF 문서 로더
│   │   ├── PDF 텍스트 추출 (PyMuPDF)
│   │   ├── OCR 처리 (Tesseract)
│   │   └── 캐싱 (pickle)
│   └── 질문 분석
│       ├── 인사말 감지
│       ├── 질문 유형 분류
│       └── 엔티티 추출
├── 데이터 전처리 계층
│   ├── 텍스트 정규화
│   ├── 특수문자 처리
│   ├── 한글/영문 토크나이제이션
│   └── 언어 감지
├── 청킹 계층 (Chunking)
│   ├── SlidingWindowChunker (기본)
│   │   └─ 512자 윈도우, 64자 오버랩
│   ├── NumericChunker (숫자 보존)
│   │   └─ 수치 정보 경계 감지
│   └── PageBasedChunker (페이지 기반)
│       └─ 문서 구조 유지
├── 임베딩 계층
│   ├── 임베더 선택
│   │   ├── HuggingFace (기본)
│   │   ├── OpenAI (프리미엄)
│   │   └── 로컬 모델
│   ├── 배치 임베딩
│   ├── 병렬 처리
│   └── 캐싱
├── 검색 계층
│   ├── 하이브리드 검색
│   │   ├── BM25 (키워드) 정확도: 0.4
│   │   └── Vector 검색 (의미) 정확도: 0.6 (동적 조정)
│   ├── 벡터 DB (Chroma)
│   │   ├── 저장소: SQLite 기반
│   │   ├── 임베딩: 768D
│   │   └── 메타데이터 필터링
│   └── 결과 점수 정규화
├── 필터링 계층
│   ├── ContextFilter
│   │   ├── 신뢰도 기반 필터링
│   │   ├── 길이 검증
│   │   └─ 관련성 필터링
│   ├── Deduplicator
│   │   └─ 중복 제거 (임베딩 기반)
│   └── GuardrailChecker
│       ├─ 안전성 검사
│       ├─ 도메인 적절성 확인
│       └─ 할루시네이션 감지
├── 리랭킹 계층
│   ├── Reranker (Cross-Encoder)
│   │   ├─ 모델: 다국어 reranker
│   │   ├─ 재정렬 상위 K개
│   │   └─ 최종 점수 계산
│   └── 점수 캘리브레이션
│       └─ 신뢰도 조정
├── 답변 생성 계층
│   ├── PromptBuilder
│   │   ├─ 질문 유형별 프롬프트
│   │   ├─ 컨텍스트 선택
│   │   └─ 템플릿 관리
│   ├── LLM (Ollama)
│   │   ├─ 모델: Qwen2.5 (3B, Q4_K_M)
│   │   ├─ 로컬 실행
│   │   └─ GPU 지원
│   ├── AnswerGenerator
│   │   ├─ 답변 생성
│   │   ├─ 폴백 메커니즘
│   │   └─ 신뢰도 계산
│   └── 답변 후처리
│       ├─ 포맷팅
│       ├─ 참고문헌 추가
│       └─ 메타데이터 생성
├── 평가 계층
│   ├── 4가지 평가 체계
│   │   ├─ 기본 Score (v5 방식)
│   │   ├─ 도메인 특화 평가
│   │   ├─ RAG 핵심 지표
│   │   │   ├─ Faithfulness
│   │   │   ├─ Answer Correctness
│   │   │   └─ Context Precision
│   │   └─ 학술 표준 지표
│   │       ├─ F1 Score
│   │       ├─ ROUGE-L
│   │       ├─ BLEU
│   │       └─ Exact Match
│   └── 메트릭 수집
│       ├─ 응답 시간
│       ├─ 신뢰도
│       └─ 소스 정보
└── API 엔드포인트
    ├── POST /ask
    ├── GET /status
    ├── GET /healthz
    ├── POST /process-pdfs
    ├── POST /api/pdf/notify-upload
    ├── POST /api/text/add-to-vectordb
    └── POST /api/pdf/extract-and-embed
```

---

## 3. 기능 흐름 상세 설명

### 3.1 사용자 인증 흐름

```
[Step 1] 사용자 입력
    사용자가 username/password 입력
         ↓
[Step 2] 회원가입 요청 (선택사항)
    POST /api/auth/register/
    - 유효성 검증 (영문/숫자, 길이 등)
    - 비밀번호 해싱 (bcrypt)
    - 데이터베이스 저장
         ↓
[Step 3] 로그인 요청
    POST /api/auth/login/
    - 사용자 확인
    - 비밀번호 검증
    - JWT 토큰 발급
      * Access Token (60분 유효)
      * Refresh Token (7일 유효)
         ↓
[Step 4] 토큰 사용
    Request Header: Authorization: Bearer <token>
    - 토큰 검증 (서명, 만료)
    - 권한 확인
    - 요청 처리 또는 거부
         ↓
[Step 5] 토큰 갱신 (만료 시)
    POST /api/auth/refresh/
    - Refresh Token으로 새 Access Token 발급
    - 사용자 세션 유지
         ↓
[결과] 사용자 세션 유지 및 리소스 접근 가능
```

---

### 3.2 교통 데이터 조회 흐름

```
[Step 1] 프론트엔드 사용자 요청
    지도에서 특정 교차로 클릭 또는 검색
         ↓
[Step 2] 백엔드 API 요청
    GET /api/traffic/intersections/{id}
    Header: Authorization: Bearer <token>
         ↓
[Step 3] 권한 검증
    Django JWT 미들웨어
    - 토큰 유효성 확인
    - 사용자 인증 상태 확인
         ↓
[Step 4] 데이터베이스 조회
    Model Query:
    - Intersection 테이블 조회
    - 관련 TrafficVolume 데이터 조회
    - Incident 정보 조회
    - FlowMetrics 데이터 조회
         ↓
[Step 5] 응답 직렬화
    Pydantic 스키마로 변환
    - 필수 필드만 포함
    - 타입 검증
    - null 처리
         ↓
[Step 6] 프론트엔드에 반환
    HTTP 200 OK
    Content-Type: application/json
    {
        "id": 1,
        "latitude": 37.xxx,
        "longitude": 127.xxx,
        "traffic_volume": {...},
        "incidents": [...],
        "created_at": "2025-01-01T00:00:00Z",
        "updated_at": "2025-01-01T00:00:00Z"
    }
         ↓
[Step 7] 프론트엔드 렌더링
    React 컴포넌트 업데이트
    - 지도에 마커 표시
    - 상세 정보 패널 업데이트
    - 차트/그래프 렌더링
         ↓
[결과] 사용자는 교차로의 실시간 교통 정보 확인
```

---

### 3.3 RAG 챗봇 질문 답변 흐름

```
[Step 1] 사용자 질문 입력
    "고산 정수장 AI 플랫폼 URL은?"
         ↓
[Step 2] 질문 분석 (QuestionAnalyzer)
    - 인사말 판단
    - 질문 유형 분류 (기술, 정의, 비교 등)
    - 엔티티 추출 (주제, 대상)
    - 결과: 인사말이면 즉시 응답, 아니면 Step 3 진행
         ↓
[Step 3] 컨텍스트 검색 (HybridRetriever)
    
    3-1) BM25 검색 (키워드 매칭, 가중치: 0.4)
         - 색인된 문서에서 키워드 일치도 계산
         - Top-K 문서 반환
    
    3-2) Vector 검색 (의미 유사도, 가중치: 0.6)
         - 질문을 768D 벡터로 임베딩
         - Chroma 벡터 DB에서 유사 청크 검색
         - 코사인 유사도 계산
         - Top-K 청크 반환
    
    3-3) 결과 병합
         - BM25 점수와 Vector 점수 가중 평균
         - 중복 제거
         - 점수 순 정렬
         - 상위 50개 반환
         ↓
[Step 4] 컨텍스트 필터링 (ContextFilter)
    - 신뢰도 기반 필터링 (점수 < 0.3 제거)
    - 길이 검증 (너무 짧은 청크 제거)
    - 관련성 필터링 (질문과의 관련도 재확인)
    - 결과: 유효한 청크 20개 선택
         ↓
[Step 5] 중복 제거 (Deduplicator)
    - 임베딩 기반 유사도 계산
    - 중복 청크 감지 (유사도 > 0.95)
    - 중복 제거 후 상위 순위 유지
    - 결과: 15개 고유 청크
         ↓
[Step 6] 안전성 확인 (GuardrailChecker)
    - 도메인 적절성 검사
    - 할루시네이션 가능성 감지
    - 안전 필터 통과
    - 결과: 검증 통과/실패
         ↓
[Step 7] 컨텍스트 리랭킹 (Reranker)
    - Cross-Encoder 모델로 재정렬
    - 질문-청크 쌍의 관련도 재계산
    - 최종 점수 부여 (0-1)
    - 상위 5개 청크 선택
    - 신뢰도 조정 (점수 정규화)
         ↓
[Step 8] 프롬프트 구성 (PromptBuilder)
    - 질문 유형에 따른 템플릿 선택
    - 상위 5개 컨텍스트 삽입
    - 도메인 사전 참고
    - 최종 프롬프트:
    
    "다음 문서를 참고하여 질문에 답변하세요.
     [컨텍스트 1]
     [컨텍스트 2]
     ...
     질문: 고산 정수장 AI 플랫폼 URL은?"
         ↓
[Step 9] 답변 생성 (AnswerGenerator + LLM)
    - Ollama 서버 호출 (Qwen2.5:3B)
    - 모델명: qwen2.5:3b-instruct-q4_K_M
    - 온도: 0.3 (안정성)
    - 최대 토큰: 500
    - 스트리밍 응답 처리
    - 결과: "AI플랫폼 URL은 http://..."
         ↓
[Step 10] 답변 후처리
    - 포맷팅 (줄바꿈, 구두점)
    - 참고문헌 추가
      * 인용한 청크 식별
      * 문서명, 페이지 번호 기록
    - 메타데이터 생성
      * 응답 시간 기록
      * 신뢰도 점수 계산
      * 사용된 리소스 추적
         ↓
[Step 11] 평가 (4가지 평가 체계)
    - 기본 Score (v5 방식)
    - 도메인 특화 평가 (숫자/단위 정확도)
    - RAG 핵심 지표
      * Faithfulness: 컨텍스트 일관성
      * Answer Correctness: 답변 정확도
      * Context Precision: 컨텍스트 정밀도
    - 학술 표준 지표 (F1, ROUGE-L, BLEU)
         ↓
[Step 12] 응답 반환
    HTTP 200 OK
    {
        "answer": "고산 정수장 AI 플랫폼 URL은 http://...",
        "confidence": 0.87,
        "sources": [
            {
                "text": "AI플랫폼 바로가기: http://...",
                "score": 0.92,
                "filename": "정수장_운영_매뉴얼.pdf",
                "page": 15
            },
            ...
        ],
        "metrics": {
            "total_time_ms": 2100,
            "retrieval_time_ms": 800,
            "generation_time_ms": 1300,
            "sources_count": 5
        }
    }
         ↓
[Step 13] 프론트엔드 렌더링
    - 채팅 인터페이스에 답변 표시
    - 신뢰도 바 표시
    - 참고문헌 링크 제공
    - 피드백 버튼 (유용함/유용하지 않음)
         ↓
[결과] 사용자는 AI 챗봇으로부터 정확한 답변 획득
```

---

### 3.4 PDF 문서 처리 및 임베딩 흐름

```
[Step 1] 사용자가 백엔드를 통해 PDF 업로드
    POST /api/documents/upload
    or
    백엔드가 PDF 파일을 data/ 디렉토리에 배치
         ↓
[Step 2] 백엔드에서 FastAPI 챗봇에 알림
    POST /api/pdf/notify-upload
    {
        "filename": "정책_자료.pdf",
        "file_path": "/app/data/정책_자료.pdf",
        "source": "backend_api"
    }
         ↓
[Step 3] PDF 텍스트 추출
    - PyMuPDF 사용 (기본)
    - PDF 객체 열기
    - 페이지별 텍스트 추출
    - 한글/영문 혼합 처리
    - 추출 실패 시 Tesseract OCR 사용
      * 페이지를 이미지로 변환
      * Tesseract로 텍스트 인식 (한글+영문)
      * 해상도 2배 확대 (정확도 향상)
         ↓
[Step 4] 텍스트 정규화 및 전처리
    - 특수문자 정리
    - 여러 공백 제거
    - 줄바꿈 정규화
    - 반복 텍스트 제거
    - 결과: 정제된 텍스트
         ↓
[Step 5] OCR 후처리 (OCRCorrector)
    - 오류 수정 (ㄹ->l 등)
    - 한글 오류 보정
    - 일반 텍스트 정규화
         ↓
[Step 6] 청킹 (Chunking)
    
    6-1) SlidingWindowChunker (기본)
         - 윈도우 크기: 512자
         - 오버랩: 64자
         - 토큰 기반 경계 조정
    
    6-2) NumericChunker (숫자 보존)
         - 수치, 날짜, URL 감지
         - 청크 경계를 수치 정보 주변에 설정
         - 단위 정보 보존 (%, ℃ 등)
    
    6-3) 청크 생성
         - 각 청크: Chunk 객체
         - 포함 정보:
           * doc_id: 문서 고유 ID
           * filename: 파일명
           * page: 페이지 번호
           * text: 청크 텍스트
           * metadata: 추가 정보 (타입, 신뢰도)
    
    결과: 한 문서에서 200-500개 청크 생성
         ↓
[Step 7] 청크 임베딩 (배치 처리)
    - 모든 청크를 수집
    - 배치 크기: 32개
    - HuggingFace 임베더 사용
      * 모델: sentence-transformers/multilingual-e5-large
      * 출력: 768D 벡터
      * 병렬 처리 (4 workers)
    - 각 청크마다 768D 벡터 생성
         ↓
[Step 8] 벡터 DB에 저장 (Chroma)
    - Chroma SQLite 컬렉션 생성/업데이트
    - 저장 정보:
      * 청크 텍스트
      * 768D 벡터
      * 메타데이터 (filename, page 등)
    - 유사도 검색 인덱스 생성
    - 인덱싱 완료
         ↓
[Step 9] 캐시 업데이트
    - pickle 파일로 청크 저장
    - 경로: /data/chunks_cache.pkl
    - 다음 서버 시작 시 빠른 로드
         ↓
[Step 10] 파이프라인 재초기화
    - 모든 청크 (기존 + 신규) 로드
    - BM25 인덱스 재구성
    - 벡터 DB 인덱스 업데이트
         ↓
[Step 11] 완료 응답
    HTTP 200 OK
    {
        "status": "success",
        "message": "PDF 파일이 성공적으로 처리되었습니다",
        "details": {
            "pdf_processed": true,
            "extracted_text_length": 15000,
            "chunks_created": 35,
            "total_chunks": 1250,
            "processing_time": 12.5
        }
    }
         ↓
[결과] 이제 사용자는 새로운 문서 내용에 대해 챗봇에 질문 가능
```

---

## 4. 데이터 모델 구조

### 4.1 핵심 데이터 타입

```
Chunk (청크 - 기본 단위)
├── doc_id: 문서 고유 ID (문자열)
├── filename: 파일명 (예: "정책_자료.pdf")
├── page: 페이지 번호 (정수)
├── start_offset: 시작 위치 (정수)
├── length: 길이 (정수)
├── text: 텍스트 내용 (문자열, 최대 512자)
├── embedding: 768D 벡터 (선택사항, 리스트)
└── metadata: 추가 정보 (딕셔너리)
    ├── source: 출처
    ├── confidence: 신뢰도
    └── extracted_at: 추출 시간

Answer (답변)
├── text: 답변 텍스트 (문자열)
├── confidence: 신뢰도 (0~1, 실수)
├── sources: 소스 청크 리스트
│   └── 각 항목: RetrievalResult
├── metrics: 평가 메트릭 (딕셔너리)
│   ├── total_time_ms: 총 처리 시간 (밀리초)
│   ├── retrieval_time_ms: 검색 시간
│   ├── generation_time_ms: 생성 시간
│   └── sources_count: 사용된 소스 수
└── fallback_used: 폴백 여부 (불리언)

RetrievalResult (검색 결과)
├── score: 관련성 점수 (0~1, 실수)
├── rank: 순위 (정수)
└── chunk: 청크 객체 (Chunk)

User (사용자)
├── id: 고유 ID (정수)
├── username: 사용명 (문자열, 고유)
├── password: 해싱된 비밀번호 (문자열)
├── email: 이메일 (문자열, 선택사항)
├── created_at: 생성 시간 (타임스탬프)
└── updated_at: 수정 시간 (타임스탬프)

Intersection (교차로)
├── id: 고유 ID (정수)
├── name: 교차로명 (문자열)
├── latitude: 위도 (실수)
├── longitude: 경도 (실수)
├── road_names: 도로명 리스트 (배열)
├── created_at: 생성 시간 (타임스탬프)
└── updated_at: 수정 시간 (타임스탬프)

TrafficVolume (교통량)
├── id: 고유 ID (정수)
├── intersection_id: 교차로 외래키 (정수)
├── timestamp: 측정 시간 (타임스탬프)
├── vehicle_count: 차량 수 (정수)
├── average_speed: 평균 속도 (실수, km/h)
└── congestion_level: 혼잡도 (0~1, 실수)

Incident (사건)
├── id: 고유 ID (정수)
├── intersection_id: 교차로 외래키 (정수)
├── type: 사건 유형 (문자열, 사고/정체/공사 등)
├── description: 설명 (문자열)
├── severity: 심각도 (정수, 1~5)
├── reported_by: 보고자 (문자열)
├── created_at: 보고 시간 (타임스탬프)
└── resolved: 해결 여부 (불리언)

EvaluationMetrics (평가 지표)
├── faithfulness: 신앙성 (0~1, 실수)
├── correctness: 정확도 (0~1, 실수)
├── precision: 정밀도 (0~1, 실수)
├── recall: 재현율 (0~1, 실수)
├── f1_score: F1 점수 (0~1, 실수)
├── rouge_l: ROUGE-L 점수 (0~1, 실수)
└── processing_time: 처리 시간 (초, 실수)
```

---

## 5. 배포 및 실행 구조

### 5.1 Docker Compose 구성

```
docker-compose.gpu.yml
├── Frontend Service (React)
│   ├── 이미지: node:18-alpine
│   ├── 포트: 3000
│   ├── 바인드 마운트: src/, public/
│   └── 명령: npm start
│
├── Backend Service (Django)
│   ├── 이미지: python:3.10
│   ├── 포트: 8001
│   ├── 의존성: MySQL
│   ├── 바인드 마운트: requirements.txt, src/
│   └── 환경변수: DATABASE_URL, SECRET_KEY
│
├── Chatbot Service (FastAPI)
│   ├── 이미지: python:3.10
│   ├── GPU: nvidia/cuda:11.8
│   ├── 포트: 8000
│   ├── 의존성: Ollama
│   ├── 바인드 마운트: data/, modules/, config/
│   └── 환경변수: OLLAMA_URL, MODEL_NAME
│
├── Ollama Service (LLM)
│   ├── 이미지: ollama/ollama:latest
│   ├── GPU: nvidia/cuda:11.8
│   ├── 포트: 11434
│   ├── 모델: qwen2.5:3b-instruct-q4_K_M
│   └── 데이터 볼륨: ollama_models/
│
├── MySQL Service (관계형 DB)
│   ├── 이미지: mysql:8.0
│   ├── 포트: 3306
│   ├── 환경변수: MYSQL_ROOT_PASSWORD, MYSQL_DATABASE
│   └── 데이터 볼륨: mysql_data/
│
└── Chroma Service (벡터 DB - 통합)
    └── FastAPI 내부 실행
```

### 5.2 런타임 포트 맵핑

```
┌──────────┬──────────────┬─────────────────────────────┐
│ 서비스    │ 포트         │ 용도                        │
├──────────┼──────────────┼─────────────────────────────┤
│ Frontend │ 3000         │ React 개발 서버             │
│ Backend  │ 8001         │ Django REST API             │
│ Chatbot  │ 8000         │ FastAPI RAG 서비스          │
│ Ollama   │ 11434        │ LLM 추론 서버               │
│ MySQL    │ 3306         │ 관계형 데이터베이스         │
│ Chroma   │ (내장)       │ 벡터 데이터베이스 (SQLite) │
└──────────┴──────────────┴─────────────────────────────┘
```

---

## 6. 시스템 성능 지표

### 6.1 Chatbot v6 성능 (현재)

```
성능 메트릭
├── 도메인 특화 점수: 94.3%
│   └─ v5 대비 +7.3%p 향상
├── 숫자 정확도: 89.5%
│   └─ v5 대비 +12.1%p 향상
├── 단위 정확도: 92.8%
│   └─ v5 대비 +8.7%p 향상
└── 평균 응답 시간: 2.1초
    ├─ 검색 시간: 0.8초 (BM25 + Vector)
    ├─ 리랭킹 시간: 0.3초
    └─ LLM 생성 시간: 1.0초

RAG 핵심 지표 (RAGAS Framework)
├── Faithfulness (신앙성): 58.3%
│   └─ 컨텍스트와 일관성
├── Answer Correctness (답변 정확도): 87.2%
│   └─ 정답과의 일치도
└── Context Precision (컨텍스트 정밀도): 76.1%
    └─ 검색된 컨텍스트의 관련도

학술 표준 지표
├── Token F1: 82.1%
│   └─ 토큰 레벨 정확도
├── ROUGE-L: 78.9%
│   └─ n-gram 기반 평가
├── BLEU: 75.4%
│   └─ 기계 번역 평가
└── Exact Match: 71.8%
    └─ 정확 일치율
```

### 6.2 시스템 요구사항

```
하드웨어 (권장 사양)
├── CPU: 8개 이상 코어
├── RAM: 16GB 이상
├── GPU: NVIDIA (CUDA 11.8+, 선택사항)
│   ├── 없음: CPU만으로 가능 (느림)
│   ├── 4GB: 기본 사용 가능
│   └── 8GB+: 최적 성능
└── 저장소: 50GB 이상 SSD

Python 환경
├── Python: 3.10 이상
├── 패키지 관리자: pip
└── 가상 환경: venv/conda

주요 의존성
├── FastAPI (API 프레임워크)
├── Django (백엔드 프레임워크)
├── PyMuPDF (PDF 텍스트 추출)
├── Tesseract (OCR)
├── Chroma (벡터 데이터베이스)
├── HuggingFace Transformers (임베딩)
├── Sentence Transformers (SBERT)
├── LangChain (오케스트레이션)
├── React (프론트엔드)
├── TypeScript (타입 안정성)
└── MySQL/SQLite (관계형 DB)

외부 서비스
├── Ollama (로컬 LLM 실행)
│   └─ Qwen2.5:3B 모델
├── HuggingFace (임베딩 모델)
└── Tesseract (OCR 엔진)
```

---

## 7. 보안 아키텍처

### 7.1 인증 및 권한 관리

```
인증 계층
├── JWT 토큰 기반 인증
│   ├── 서명 알고리즘: HS256
│   ├── Access Token: 60분 유효
│   └── Refresh Token: 7일 유효
├── 비밀번호 해싱
│   └── 알고리즘: bcrypt
└── 세션 관리
    └── 상태 비저장 (Stateless)

권한 관리
├── 역할 기반 접근 제어 (RBAC)
│   ├── 관리자 역할
│   ├── 사용자 역할
│   └── 손님 역할
├── 엔드포인트별 권한 검증
│   └── 각 API는 필요한 권한 정의
└── 리소스 레벨 접근 제어
    └── 사용자는 자신의 리소스만 접근

데이터 보안
├── 암호화
│   ├── 저장 시: 민감 정보 암호화
│   ├── 전송 시: HTTPS (프로덕션)
│   └── 해싱: 비밀번호, 토큰
├── SQL 인젝션 방지
│   └── ORM 사용 (Django ORM, SQLAlchemy)
└── CORS 설정
    └── 프론트엔드 도메인만 허용

로깅 및 모니터링
├── 감사 로그
│   ├── 사용자 로그인/로그아웃
│   ├── 리소스 접근
│   └── 데이터 수정
├── 에러 추적
│   ├── 스택 추적
│   ├── 요청/응답 로그
│   └── 성능 메트릭
└── 알람
    ├── 의심스러운 활동 감지
    ├── 에러율 초과
    └── 가용성 저하
```

---

## 8. 확장성 및 최적화 전략

### 8.1 수평 확장 (Horizontal Scaling)

```
멀티 프로세스 확장
├── FastAPI Workers
│   ├── gunicorn과 uvicorn 사용
│   ├── 워커 수: CPU 코어 x 2-4
│   └── 로드 밸런싱: Round-Robin
├── Django Workers
│   ├── uwsgi/gunicorn 사용
│   ├── 워커 수: CPU 코어 + 1
│   └── 요청 대기열 관리
└── React 서빙
    ├── Nginx 리버스 프록시
    ├── 정적 파일 캐싱
    └── CDN 연계

데이터베이스 확장
├── 마스터-슬레이브 레플리케이션
│   ├── 쓰기: 마스터 DB
│   └── 읽기: 슬레이브 DB (분산)
├── 샤딩 (필요시)
│   └─ 사용자/문서별 분할
└── 캐싱 계층 추가
    └─ Redis (세션, 임베딩 캐시)

벡터 DB 확장
├── Chroma 클러스터링
│   └─ 여러 벡터 DB 인스턴스
├── 또는 프로덕션 벡터 DB 도입
│   ├── Pinecone (클라우드)
│   ├── Weaviate (오픈소스)
│   └── Milvus (오픈소스)
└── 인덱스 최적화
    └─ HNSW, IVF 등
```

### 8.2 성능 최적화

```
요청 레벨 최적화
├── 배치 처리
│   └─ 여러 질문 한 번에 처리
├── 비동기 처리 (async/await)
│   └─ 블로킹 작업 제거
├── 타임아웃 설정
│   └─ 무한 대기 방지
└── 캐싱
    ├── 응답 캐싱
    ├── 쿼리 캐싱
    └── 임베딩 캐싱

데이터 레벨 최적화
├── 청크 크기 최적화
│   ├── 너무 작음: 컨텍스트 부족
│   └─ 너무 큼: 불필요한 정보 포함
├── 메타데이터 인덱싱
│   └─ 빠른 필터링
├── 벡터 DB 인덱스
│   └─ 검색 성능 향상
└── 통계 분석
    └─ 쿼리 플랜 최적화

모델 레벨 최적화
├── 모델 양자화
│   ├── Q4_K_M (4-bit)
│   └─ 성능 유지하며 크기 감소
├── 경량 모델 사용
│   └─ Qwen2.5:3B (큰 모델 대신)
├── 프롬프트 길이 제한
│   └─ 토큰 수 최소화
└── 배치 임베딩
    └─ 효율성 증가

인프라 레벨 최적화
├── GPU 활용
│   ├── CUDA 병렬 처리
│   └─ 메모리 최적화
├── 메모리 관리
│   ├── 가비지 컬렉션
│   └─ 메모리 누수 방지
├── 디스크 I/O 최적화
│   ├── SSD 사용
│   └─ 캐싱
└── 네트워크 최적화
    ├── 압축
    └─ CDN 활용
```

---

## 9. 운영 및 유지보수

### 9.1 배포 단계

```
개발 환경 (Local)
├── 코드 작성 및 단위 테스트
├── 로컬 Docker Compose 실행
├── 통합 테스트
└── 커밋 및 푸시

스테이징 환경
├── 전체 시스템 통합
├── 성능 테스트 (부하 테스트)
├── 보안 검사
├── 회귀 테스트
└── 체크리스트 검증

프로덕션 환경
├── 블루-그린 배포
│   ├── 기존 시스템 (Blue)
│   ├── 새로운 시스템 (Green)
│   └─ 트래픽 전환
├── 헬스 체크
├── 모니터링 활성화
└── 롤백 준비

운영 & 유지보수
├── 정기 백업
│   ├── 데이터베이스 백업 (일 1회)
│   ├── 파일 저장소 백업
│   └─ 암호화된 저장
├── 로그 분석
│   ├── 에러 추적
│   ├── 성능 분석
│   └─ 사용 패턴 분석
├── 성능 튜닝
│   └─ 병목 지점 개선
├── 보안 업데이트
│   ├── 의존성 업데이트
│   └─ 취약점 패치
└── 사용자 지원
    └─ 버그 리포트 처리
```

### 9.2 모니터링 지표

```
실시간 메트릭
├── 응답 시간 분포
│   ├── 평균: 2-3초 목표
│   ├── P95: 5초 미만
│   └─ P99: 10초 미만
├── 에러율
│   ├── 목표: 1% 미만
│   └─ 경고: 5% 초과
├── 처리량 (QPS: Queries Per Second)
│   └─ 현재 용량 대비 80% 이상 도달 시 스케일링
├── 리소스 사용률
│   ├── CPU: 70% 초과 시 경고
│   ├── 메모리: 80% 초과 시 경고
│   ├── 디스크: 85% 초과 시 경고
│   └─ 네트워크: 대역폭 모니터링
├── 토큰 사용량 (LLM)
│   └─ 예상 비용 대비 추적
├── 임베딩 캐시 히트율
│   └─ 목표: 60% 이상
└── 사용자 활동
    ├── 동시 사용자
    ├── 일 활성 사용자
    └─ 질문 유형 분포

경고 규칙
├── 응답 시간 > 5초
├── 에러율 > 5%
├── CPU > 90% (5분 이상)
├── 메모리 > 90% (3분 이상)
├── 디스크 > 95%
├── 가용성 < 99.5% (1시간 기준)
├── 데이터베이스 연결 풀 고갈
└── 벡터 DB 응답 시간 > 2초
```

---

## 10. 결론 및 권장사항

### 10.1 시스템 강점

```
아키텍처 측면
├── 명확한 마이크로서비스 분리
│   └─ 각 서비스의 책임이 명확함
├── 독립적인 확장성
│   └─ 필요한 부분만 스케일 가능
├── 느슨한 결합
│   └─ 서비스 간 독립적 업데이트 가능
└── 높은 재사용성
    └─ 모듈화된 컴포넌트

기술 스택 측면
├── 현대적인 기술 사용
│   ├── React + TypeScript (프론트엔드)
│   ├── Django + Ninja (백엔드)
│   └─ FastAPI (AI 서비스)
├── 오픈소스 생태계
│   └─ 커뮤니티 지원 활발
└── 클라우드 친화적
    └─ Docker 기반 배포

기능 측면
├── 지능형 RAG 챗봇
│   ├── 도메인 특화 학습
│   └─ 94.3% 정확도
├── 하이브리드 검색
│   ├── 키워드 + 의미 결합
│   └─ 최적화된 가중치
├── 다층 필터링
│   ├── 신뢰도 보장
│   └─ 안전성 검사
└── 포괄적 평가
    ├── 4가지 평가 체계
    └─ 성능 추적 가능

보안 측면
├── 강력한 인증
│   ├── JWT 토큰
│   └─ Refresh 메커니즘
├── 권한 관리
│   ├── RBAC 구현
│   └─ 리소스 레벨 제어
├── 데이터 보호
│   ├── 암호화
│   └─ SQL 인젝션 방지
└── 감사 추적
    └─ 모든 중요 작업 기록
```

### 10.2 현실적 제약사항

```
확장성 제약
├── 단일 Ollama 인스턴스
│   └─ 동시 요청 1-2개만 처리 가능
├── SQLite 기반 벡터 DB
│   ├─ 대규모 데이터 성능 저하
│   └─ 동시성 제한
└── Django 동기 처리
    └─ I/O 집약적 작업 효율 낮음

성능 병목
├── RAG 파이프라인 (2.1초)
│   ├─ 사용자 경험에 직접 영향
│   ├─ 특히 LLM 생성 시간 (1초+)
│   └─ 실시간 채팅에는 긴 편
├── 임베딩 연산
│   └─ 배치 처리 필요
├── 벡터 검색 인덱싱
│   └─ 대규모 데이터셋에서 느림
└── PDF 처리
    └─ OCR 시간 소비 많음

리소스 소비
├── 메모리 요구
│   ├── Python 기본: 2-4GB
│   ├── 모델 로드: 5-8GB
│   └─ 임베딩 캐시: 2-3GB
├── GPU 필요성
│   ├── 없어도 작동 (매우 느림)
│   ├── 권장: 4GB VRAM 이상
│   └─ 최적: 8GB+ VRAM
└── 저장소 요구
    ├── 모델 파일: 10GB+
    ├── 벡터 DB: 데이터에 따라 가변
    └─ 로그 파일: 정기 정리 필요

운영 복잡도
├── 4개 서비스 + 3개 DB 관리
│   └─ 각 컴포넌트 별도 모니터링
├── 의존성 관리
│   ├── Python: pip 의존성
│   ├── Node: npm 의존성
│   └─ 시스템: 외부 서비스 (Ollama, Tesseract)
├── 데이터 동기화
│   ├── 여러 저장소 간 일관성
│   └─ 캐시 무효화 전략
└── 버전 관리
    └─ 마이그레이션 복잡도
```

### 10.3 개선 권장사항 (우선순위)

```
즉시 개선 (1-2주)
├── Redis 도입
│   ├── 임베딩 결과 캐싱
│   ├── 세션 저장소
│   └─ 응답 시간 50% 단축 예상
├── 배치 처리 자동화
│   ├── 여러 질문 동시 처리
│   └─ 처리량 2-3배 증가 예상
├── 로깅 개선
│   ├── 구조화된 로그
│   └─ 성능 모니터링 강화
└── 테스트 커버리지 증대
    ├── 단위 테스트
    ├── 통합 테스트
    └─ 부하 테스트

단기 개선 (1-2개월)
├── 모델 최적화
│   ├── 프롬프트 길이 제한
│   ├── 토큰 수 최소화
│   └─ 온도 파라미터 튜닝
├── 검색 알고리즘 개선
│   ├── 하이브리드 가중치 재조정
│   ├── 리랭킹 모델 업그레이드
│   └─ 필터링 규칙 강화
├── 다중 Ollama 인스턴스
│   ├── 로드 밸런싱 구성
│   └─ 동시 처리량 증가
└── 프로덕션 벡터 DB 도입
    ├── Pinecone 또는 Weaviate
    └─ 확장성 및 성능 대폭 향상

중기 개선 (3-6개월)
├── Kubernetes 도입
│   ├── 자동 스케일링
│   ├── 컨테이너 오케스트레이션
│   └─ 고가용성 구성
├── 마이크로프론트엔드 아키텍처
│   ├── 독립적 배포
│   └─ 팀 간 독립성
├── 다국어 지원 강화
│   ├── 번역 API 통합
│   └─ 언어별 LLM 모델 추가
└── 고급 분석 도구
    ├── 대시보드 강화
    ├── 트렌드 분석
    └─ 예측 분석

장기 개선 (6개월+)
├── 엔터프라이즈급 기능
│   ├── 강화된 보안
│   ├── 규정 준수 (GDPR, 개인정보보호)
│   └─ SLA 보장
├── 멀티테넌시 지원
│   ├── 사용자별 격리
│   └─ 맞춤형 설정
├── 실시간 협업 기능
│   ├── WebSocket 기반
│   └─ 실시간 알림
└── 고급 AI 기능
    ├── 사용자 맞춤형 응답
    ├── 답변 학습 및 개선
    └─ 다중 모드 지원
```

---

## 최종 요약

이 문서는 IFRO 서울 교통 플랫폼의 **기술 구조**와 **기능 흐름**을 체계적으로 설명합니다.

**핵심 특징**:
- 4층 아키텍처 (클라이언트, API, AI, 데이터)
- 3개의 독립적인 마이크로서비스
- 지능형 RAG 기반 챗봇 (94.3% 정확도)
- 현대적 기술 스택 (React, Django, FastAPI)
- 강력한 보안 (JWT, RBAC, 감사 로그)
- 포괄적 평가 체계 (4가지 평가 방식)

**현실적 제약**:
- 단일 Ollama 인스턴스 (확장성 제한)
- SQLite 벡터 DB (대규모 데이터 성능 저하)
- 2.1초 응답 시간 (실시간 채팅에는 긴 편)
- 높은 리소스 소비 (16GB+ 메모리, 선택적 GPU)
- 운영 복잡도 높음 (4 서비스 + 3 DB)

**개선 로드맵**:
우선순위에 따라 즉시, 단기, 중기, 장기 개선 사항을 제시하여 지속적인 시스템 진화를 도모합니다.

---

**최종 업데이트**: 2025년 11월 11일
**버전**: 2.0 (이모지 제거, 기능 흐름 통합)
**상태**: 프로덕션 배포 가능
