# Traffic Policy Q&A System with Context Selection Optimization

졸업 논문용 기술 보고서. 기본 RAG(v1~v6) 실험을 거쳐 온톨로지 기반 추론으로 확장한 과정, 설계 이유, 성능 목표를 정리한다.

## Project outline
- 문제의식: 단순 유사도 기반 RAG는 정책·규정의 인과/조건을 충분히 반영하지 못한다. 동일 개념을 다른 표현으로 쓰는 경우 검색 오차가 커지고, 긴 문맥에서는 불필요한 컨텍스트가 과다 사용된다.
- 목표: (1) 필요한 컨텍스트만 선택해 효율을 높이고, (2) 관계·인과를 이해하는 온톨로지 기반 QA를 구축하며, (3) 신뢰도 관리와 안전한 폴백을 통해 안정적으로 답변한다.
- 설계 원칙:
  - 최소 의미 단위(Fragment)로 분해해 누락·혼선을 줄인다.
  - NER·정규화로 질문/문서 표현 차이를 줄여 같은 개념을 맞춘다.
  - 관계 추출로 “누가-무엇을-언제/왜/어떻게”의 구조를 그래프 엣지로 모델링한다.
  - 검증 후 도메인/퍼스널을 분리 저장해 공용 지식과 개인 맥락을 동시에 관리한다.
  - 그래프 추론을 기본으로 하되 신뢰도가 낮으면 보수적 응답 또는 LLM 보조로 전환해 품질·안정성을 균형 있게 유지한다.
- 발전 경로: RAG 실험(v1~v6)을 거쳐 온톨로지 그래프 기반 multi-hop reasoning으로 확장.

## How to do it
- 문서 전처리와 개념 정합
  - Fragmentation: 문단·문장 단위로 분리해 규정/정책의 의미 단위를 보존, 긴 문맥에서 불필요한 정보 혼입을 차단.
  - NER·정규화: 질문과 문서가 다른 표현을 써도 같은 개념으로 정렬하기 위해 도메인 용어를 표준화. 이는 그래프 노드 매칭과 검색 품질을 동시에 높임.
- 관계 추출과 신뢰도 관리
  - 관계 추출: 엔티티 쌍 사이의 역할·방향(조건, 의무, 예외 등)을 edge로 기록해 인과/규칙 구조를 표현. 단순 유사도 대비 “왜/어떻게”를 설명할 근거를 확보.
  - 검증(Validation): Rule+LLM으로 관계 신뢰도를 평가해 도메인 그래프에 올릴지, 퍼스널에 둘지, 폐기할지 결정. 잘못된 관계 유입을 차단해 그래프 오염을 막음.
- 지식 저장과 분리
  - 도메인 vs 퍼스널: 공용 규정은 도메인 그래프에, 개인 맥락은 퍼스널 그래프에 저장. 퍼스널에서 검증된 승격 후보를 도메인으로 올려 확장성을 확보.
- 추론과 안전장치
  - 그래프 기반 multi-hop reasoning으로 관계·인과를 따라 답변. 신뢰도 임계 미만이면 보수적 응답 또는 LLM 보조로 전환해 품질과 안전성을 동시에 확보.
- 관찰·운영
  - relation 수, ingested fragment/edge, LLM 가용성, ready 상태를 메트릭으로 관리해 성능·안정성을 추적. 인입 실패는 명시적으로 보고해 재처리 가능하게 설계.

## Performance Goal
- 발전 경로와 효과
  - v1: 기본 chunk + 코사인 유사도 검색으로 기준선 설정 → 현재 대비 컨텍스트 사용량이 높고 인과 반영 한계 확인.
  - v2: chunk 크기/stride 최적화, 메타데이터 필터 → 노이즈 감소, 컨텍스트 사용량 축소.
  - v3: rerank(질문-청크 상호작용) → 상위 컨텍스트 품질 상승, 불필요 청크 추가 감소.
  - v4: 다중 창 검색 + late fusion → 누락 감소, 커버리지 향상.
  - v5: query rewriting/expansion → 질의 표현력 강화, 검색 회수율 향상.
  - v6: light reasoning 앙상블 → 유사도 의존도 완화, 규칙 기반 보정 도입.
  - Ontology 전환: 관계·인과를 그래프로 모델링해 multi-hop reasoning 수행 → 컨텍스트 사용량을 베이스라인 대비 ≤40%로 낮추고, 인과/조건 기반 답변 가능.
- 컨텍스트 절감: 최종 목표 베이스라인 대비 ≤40%.
- 정확도·신뢰: 신뢰도 임계 미만 시 보수적 응답/LLM 보조, reasoning trace·근거를 항상 포함해 검증 가능성 확보.
- 응답 지연: 단일 질의 2–5초(LLM 사용 시 5–8초) 내 응답.
- 무결성: 정적 가드와 충돌/드리프트 감지로 잘못된 관계 유입 차단 및 일관성 유지.

## Product in
- 처리 흐름: 문서/PDF → Fragmentation → NER·정규화 → 관계 추출 → Validation → 도메인/퍼스널 분리 저장 → 그래프 추론 → 응답(신뢰도 미달 시 폴백).
- 선택 이유:
  - NER·정규화: 표현 차이로 인한 검색·그래프 매칭 오류를 줄여 동일 개념을 정확히 연결.
  - 관계 추출: 단순 “비슷한 문장”이 아닌 규정의 인과/조건을 답변에 반영하기 위해 필수.
  - 검증 후 분리 저장: 공용/개인 지식을 구분해 충돌을 줄이고, 승격 관리를 통해 지속 확장.
  - 그래프 추론+폴백: 추론 능력을 확보하되, 신뢰 부족 시 안전한 응답을 보장.
- 성장 가능성:
  - RAG v5/v6의 query rewriting·rerank 자산을 그래프 앞단에 결합해 anchoring 품질 향상.
  - 사용자 피드백/RL로 relation·정책 가중치를 자동 보정해 자가 개선형 추론기로 발전.
  - drift/승격 이벤트와 ingest 성능을 주기 리포트화하여 운영 성숙도 제고.
